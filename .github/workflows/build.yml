name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: wrapper
      
      - run: chmod +x gradlew
      
      - name: Build Velocity plugin
        run: ./gradlew velocity:build --no-daemon
      
      - name: Build Fabric mod
        run: ./gradlew fabric:build --no-daemon
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Artifacts
          path: |
            velocity/build/libs/
            fabric/build/libs/
          retention-days: 30